# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Git Auto-Sync Rule

Whenever any changes are made to the codebase, configuration, or documentation:
- Immediately create a clear, descriptive Git commit.
- Automatically push the changes to `origin/main` (`https://github.com/idan313/parkguard.git`).
- Ensure the remote repository is always in full sync with the local codebase.
